# 2026-03-10

## Core Feature: Landing Page Audit + Overhaul

### Primary changes
- Rewrote all customer-facing copy using StoryBrand SB7 framework
  - Hero subheading: brand-centric → customer-centric
  - 6 feature titles: jargon → benefits ("Multi-Source Ingestion" → "Every source, one place")
  - Problem section: added internal problem beat, specificity to headline
  - Chat demo: removed "not hallucinations" doubt-planting
  - Integrations: differentiated from How It Works step 1
  - Final CTA: added stakes/urgency line
- Replaced Metrics section with Success/Transformation section (SB7 Element 7)
- Adopted Button.vue component for all 4 CTAs (was 3 inconsistent inline styles)

### Dependencies (out-of-scope changes required)
- Button.vue: added `lg` and `header` size variants, `target`/`rel` props
- Fixed WCAG contrast (`text-muted-foreground/50` → `/70` across site)
- Increased section padding (`py-16` → `py-20 md:py-28`)
- Removed dead CSS (`.metric-card`, `.metric-value`, `.metric-label`)

### Files changed
- `src/App.vue` (complete rewrite → shell extraction)
- `src/components/ui/Button.vue` (added sizes + props)
- `src/style.css` (removed metric CSS, updated timeline, added pill cursor)

---

## Core Feature: /docs View + Architecture Refactor

### Primary changes
- Added `/docs` route with 9 documentation sections
- Extracted landing page into `HomeView.vue`
- App.vue refactored to shell pattern (header + router-view + footer)

### Dependencies
- Router updated with `/` and `/docs` routes
- All nav `<a>` tags converted to `<router-link>`
- Sitemap updated with `/docs` entry
- DocsView code-split (lazy loaded, 20.7 KB chunk)

### Files changed
- `src/App.vue` (shell rewrite)
- `src/views/HomeView.vue` (new)
- `src/views/DocsView.vue` (new)
- `src/router/index.ts` (routes added)
- `public/sitemap.xml` (docs entry)

---

## Core Feature: SEO Audit + Fixes

### Primary changes
- Updated meta description, OG description, Twitter description to customer-centric copy
- Updated JSON-LD featureList and description
- Added `content-language` meta tag
- Added `lastmod` to sitemap

### Files changed
- `index.html`
- `public/sitemap.xml`

---

## Infra: Render Blueprint + GitHub Repo

- Added `render.yaml` (static site, security headers, immutable caching, SPA rewrite)
- Created `enfraxyz/thesisforge-landing` GitHub repo
- Initial commit + push
